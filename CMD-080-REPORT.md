# CMD-080 — SANA REAL ORDER LIFECYCLE, CHECKOUT, ORDER ID & ADMIN NOTIFICATION REPORT

**المشروع:** Sana / سناء — خدمة عملاء متجر الذيباني  
**تاريخ التنفيذ:** 25 أغسطس 2026  
**الوضع النهائي:** مكتمل ومتحقق بالكامل (COMPLETED & VERIFIED)  

---

## 1. الملخص التنفيذي (Executive Summary)

تم بحمد الله تحويل نظام المحادثة في "سناء" من التعامل مع "نية الشراء" إلى دورة حياة كاملة وحقيقية للطلب (Real Order Lifecycle) قابلة للتتبع والإدارة، مع الحفاظ الكامل على هندسة النظام المستقرة:

- **Google Sheets كمصدر الحقيقة** (Source of Truth) لبيانات المتجر والمنتجات والسياسات.
- **تخصيص الجداول القياسية `orders` و `order_items`** في البنية التحتية لتخزين الطلبات بدلاً من خلطها بجدول المنتجات.
- **إنشاء رقم طلب فريد وثابت (Order ID)** بالصيغة `ORD-YYYYMMDD-XXXX`.
- **تثبيت أسعار المنتجات وقت الطلب (Price Snapshotting)** لحماية تاريخ الطلب من تغييرات الكتالوج المستقبلي.
- **مسار شراء متكامل وشامل (Checkout Flow)**: اختيار المنتجات -> السلة -> حساب رسوم التوصيل -> عنوان التوصيل -> طريقة الدفع -> ملخص الطلب -> تأكيد العميل -> إنشاء الطلب -> إشعار الإدارة -> متابعة حالة الطلب.
- **تتبع الحالة واستمرار السياق (Context Preservation)** دون إعادة المحادثة إلى البداية.
- **تحصين من تكرار الطلبات (Idempotency)** ومنع إنشاء طلبات مكررة عند إعادة التأكيد.
- **حماية الأمان والعزل الكامل بين المشتركين والفيشة (Tenant & Store Isolation)**.

---

## 2. عناصر التنفيذ بالتفصيل (Detailed Implementation)

### 2.1. رقم الطلب الثابت والفريد (Order ID)
- تم بناء `OrderStore.generateOrderId()` لتوليد رقم طلب غير قابل للتكرار وغير مكرر.
- النمط: `ORD-YYYYMMDD-XXXX` (مثال: `ORD-20260825-0001`).
- لا يتغير بعد الإنشاء ولا يعتمد على رقم السطر في الشيت.

### 2.2. تجميد الأسعار وحفظ العناصر (Price Snapshotting & Order Items)
- عند إضافة أي منتج للطلب: يتم التقاط اسمها وسعرها في تلك اللحظة (`productNameSnapshot`, `unitPriceSnapshot`).
- تظل قيم العناصر وثمن الطلب في `order_items` ثابتة حتى لو تغير سعر المنتج في الكتالوج لاحقاً.

### 2.3. إدارة التوصيل والدفع (Delivery & Payment Configuration)
- **التوصيل**: يتم التحقق من حالة التوصيل المفعلة في المتجر وتحديد قيمة رسوم التوصيل تلقائياً (مثلاً 500 YER).
- **طرق الدفع**: استعلام طرق الدفع المفعلة (`isActive: true`). دعم الدفع عند الاستلام ("كاش عند الاستلام") وتحويل حاسب/المحفظة.
- **فصل حالة الدفع عن حالة الطلب**: الدفع عند الاستلام يضع `paymentStatus` = `UNPAID` أو `PENDING` ولا يدعي `PAID` بدون إثبات.

### 2.4. حفظ سياق المحادثة (Context Preservation & Summary)
- عند إدخال العميل للعنوان ("العنوان شارع النصر جوار المحول") أو اختيار طريقة الدفع، يتم حفظ البيانات في سلة المحادثة دون مسح المنتجات أو إعادة البدء.
- عند إجابة العميل بـ "نعم" بعد استفساره عن منتج، يتم التقط المنتج المعروض في الجولة السابقة وإضافته للسلة تلقائياً.
- **ملخص الطلب (Order Summary)**: يتم عرضه شاملاً المنتجات، والكميات، وأسعار الوحدة، ومجموع المنتجات، ورسوم التوصيل، والإجمالي النهائي، وطريقة الدفع، وعنوان التوصيل، مع السؤال المباشر: `"هل تؤكد الطلب؟"`.

### 2.5. إشعار الإدارة وفصل الإنشاء عن الإشعار (Admin Notification & Resilience)
- عند تأكيد العميل، يربط النظام بالوحدة `AdminNotifier` التي تنشئ إشعاراً في سجل الإدارة يحتوي على كافة تفاصيل الطلب بدون أسرار.
- تم فصل عملية حفظ الطلب عن إرسال الإشعار لضمان أنه في حال تعثر إرسال الإشعار لا تتأثر عملية إنشاء الطلب الحقيقي.

### 2.6. متابعة الطلب والانتقال الأمن للحالات (Order Tracking & Status Transitions)
- **متابعة الطلب**: إذا سأل العميل "أين طلبي؟" وكان رقم الطلب موجهاً في الجلسة `activeOrderId` أو مذكوراً في الرسالة، يرد النظام بحالته الحقيقية المحدثة دون مطالبة العميل بإعادة إدخال رقم الطلب.
- **مصفوفة الانتقال بين الحالات**:
  `PENDING` -> `CONFIRMED` -> `PREPARING` -> `READY_FOR_DELIVERY` -> `OUT_FOR_DELIVERY` -> `DELIVERED` أو `CANCELLED`.
  النظام يرفض التغييرات غير المنطقية (مثل التحويل من `CANCELLED` إلى `DELIVERED`).

### 2.7. التحويل البشري المثرى بسياق الطلب (Human Handoff with Order Context)
- عند طلب العميل لموظف بشري أثناء الشراء، يتم تضمين سياق الطلب بالكامل (رقم الطلب إن وجد، المنتجات، المبالغ، العنوان، طريقة الدفع) ضمن `handoffState.orderContext`.

---

## 3. نتائج الاختبارات وتغطية حالات الاستخدام (Test Suite Verification)

تم إنشاء ملف الاختبارات الشامل `src/core/cmd-080.test.ts` الذي يغطي 25 سيناريو باختبارات وحدة وتكامل متقدمة:

| # | اسم السيناريو | النتيجة |
|---|---|---|
| 1 | توليد Order ID فريد بصيغة `ORD-YYYYMMDD-XXXX` | PASS |
| 2 | حفظ الطلب في `orders` بخصائصه القياسية | PASS |
| 3 | تجميد سعر اسم المنتج وحدته وقت الطلب (Price Snapshotting) | PASS |
| 4 | حساب الإجمالي الفريد ورسوم التوصيل والإجمالي النهائي | PASS |
| 5 | حفظ عنوان التوصيل دون مسح عناصر السلة | PASS |
| 6 | حفظ طريقة الدفع دون إفساد سياق المحادثة | PASS |
| 7 | توليد ملخص الطلب الكامل قبل التأكيد | PASS |
| 8 | إنشاء الطلب بحالة `PENDING` عند تأكيد العميل | PASS |
| 9 | منع تكرار الطلب عند إعادة التأكيد (Idempotency) | PASS |
| 10 | إنشاء إشعار الإدارة عند تأكيد الطلب | PASS |
| 11 | مرونة عملية الإنشاء في حال تعثر الإشعار | PASS |
| 12 | استعلام العميل عن حالة الطلب من سياق الجلسة `activeOrderId` | PASS |
| 13 | استعلام العميل برقم طلب صريح في الرسالة | PASS |
| 14 | مطالبة العميل برقم الطلب في حال عدم وجوده | PASS |
| 15 | السماح بالتسلسل الصحيح لحالات الطلب | PASS |
| 16 | رفض الانتقالات الخاطئة بين حالات الطلب (مثل من ملغي إلى موصل) | PASS |
| 17 | استقلالية حالة الدفع عن حالة الطلب | PASS |
| 18 | إرفاق سياق الطلب عند التحويل لموظف بشري | PASS |
| 19 | رفض الوصول لطلبات مؤسسة أخرى (Tenant Isolation) | PASS |
| 20 | رفض الوصول لطلبات متجر آخر (Store Isolation) | PASS |
| 21 | إضافة المنتج المعروض للسلة عند إجابة العميل بـ "نعم" | PASS |
| 22 | تحديث الكمية وإعادة حساب المجموع بالسلة | PASS |
| 23 | الاعتماد على إعدادات رسوم التوصيل من المتجر | PASS |
| 24 | المطابقة الصحيحة لطريقة الدفع عند الاستلام | PASS |
| 25 | اختبار مسار الشراء كاملاً والتكامل النهائي لـ HaneenService | PASS |

---

## 4. التحقق المباشر على خادم Render (Live Acceptance Endpoint)

تم إنشاء نقطة تحقق مباشرة للإنتاج:
- **الرابط**: `GET /api/admin/live-order-verification`
- **الوظيفة**: تقوم باختبار إنشاء طلب حقيقي برقم فريد، وتجميد أسعار العناصر، واختبار إشعار الإدارة، وتحديث حالة الطلب، والتحقق من التزام البنية بالتنسيق القياسي للطلب.

---

## 5. الخلاصة والجاهزية للإنتاج (Verdict)

تكللت مهمة **CMD-080** بالنجاح التام:
- البناء البرمجي يمر بنجاح (`compile_applet` PASSED).
- جميع الاختبارات الـ 25 تعمل وتمر بنجاح 100%.
- لم يتم المساس ببنية Google Sheets أو نموذج Gemini أو الميزات الحالية المستقرة.
- النظام جاهز بالكامل للعمل المباشر على بيئة Production.
